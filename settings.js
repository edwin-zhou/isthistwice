var config = {
    BATCH_SIZE: 30,
    BATCHES_PER_EPOCH: 25,
    EPOCHS: 20,
    VALIDATION_SIZE: 100,
    IMG_SIZE: [256,256],
    LABELS: ['chae', 'others+t'],
    MODEL_NAME: 'modelcto2'
}

module.exports = config